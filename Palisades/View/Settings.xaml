<Window x:Class="Palisades.View.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:Palisades.ViewModel"
        mc:Ignorable="d"
        Title="Settings" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize">
    <Window.DataContext>
        <viewModel:SettingsViewModel />
    </Window.DataContext>
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{Binding SettingsPage}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="{Binding Language}" FontSize="16" FontWeight="Bold"/>
            <ComboBox Margin="0,5,0,0" 
                      ItemsSource="{Binding AvailableLanguages}" 
                      SelectedValue="{Binding SelectedLanguage, Mode=TwoWay}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Code" />
        </StackPanel>
    </Grid>
</Window>