<Application x:Class="Palisades.App"  
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             xmlns:vm="clr-namespace:Palisades.ViewModel"
             xmlns:local="clr-namespace:Palisades">
    <Application.Resources>
        <ResourceDictionary>
            <ContextMenu x:Key="SysTrayMenu" x:Shared="false">
                <MenuItem Command="{Binding ShowWindowCommand}" Header="{Binding SettingsText}" />
                <MenuItem Command="{Binding ExitApplicationCommand}" Header="{Binding ExitText}" />
            </ContextMenu>
            <tb:TaskbarIcon
                x:Key="Taskbar"
                ContextMenu="{StaticResource SysTrayMenu}"
                DoubleClickCommand="{Binding ShowWindowCommand}"
                IconSource="pack://application:,,,/Resource/icon.ico"
                ToolTipText="Palisades">
                <tb:TaskbarIcon.DataContext>
                    <vm:NotifyIconViewModel />
                </tb:TaskbarIcon.DataContext>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>
